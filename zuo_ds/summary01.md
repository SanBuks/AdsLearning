### 三种简单排序
- 选择排序
- 冒泡排序
- 插入排序
- 检测排序正确工具类
- 二分查找(0136)

### 异或
- 本质是无进位相加
- 交换律结合律(证明)
- (0, 1, N) ^ N
- in-place swap
- a & (~a + 1) / a & -a 获取最右侧 1 (注意有符号数最小值)

```c++
// 打印二进制位数
int a = 3;
std::bitset<sizeof(int)*8> b(a);
std::cout << b << "\n";
```